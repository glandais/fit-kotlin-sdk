# The generated profile is large: ~325 source files, ~200 enums and 123 message
# classes. The default 512m heap is not enough for the Kotlin compiler daemon.
org.gradle.jvmargs=-Xmx3g
org.gradle.caching=true
kotlin.code.style=official
