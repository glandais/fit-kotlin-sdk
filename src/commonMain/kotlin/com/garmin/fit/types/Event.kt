/////////////////////////////////////////////////////////////////////////////////////////////
// Copyright 2026 Garmin International, Inc.
// Licensed under the Flexible and Interoperable Data Transfer (FIT) Protocol License; you
// may not use this file except in compliance with the Flexible and Interoperable Data
// Transfer (FIT) Protocol License.
/////////////////////////////////////////////////////////////////////////////////////////////
// ****WARNING****  This file is auto-generated!  Do NOT edit this file.
// Profile Version = 21.213.0Release
// Tag = production/release/21.213.0-0-g9e10ea9c
/////////////////////////////////////////////////////////////////////////////////////////////

package com.garmin.fit.types

/**
 * FIT profile type `event`.
 */
public enum class Event(
    public val value: UByte,
) {
    TIMER(0u), // Group 0. Start / stop_all
    WORKOUT(3u), // start / stop
    WORKOUT_STEP(4u), // Start at beginning of workout. Stop at end of each step.
    POWER_DOWN(5u), // stop_all group 0
    POWER_UP(6u), // stop_all group 0
    OFF_COURSE(7u), // start / stop group 0
    SESSION(8u), // Stop at end of each session.
    LAP(9u), // Stop at end of each lap.
    COURSE_POINT(10u), // marker
    BATTERY(11u), // marker
    VIRTUAL_PARTNER_PACE(12u), // Group 1. Start at beginning of activity if VP enabled, when VP pace is changed during activity or VP enabled mid activity. stop_disable when VP disabled.
    HR_HIGH_ALERT(13u), // Group 0. Start / stop when in alert condition.
    HR_LOW_ALERT(14u), // Group 0. Start / stop when in alert condition.
    SPEED_HIGH_ALERT(15u), // Group 0. Start / stop when in alert condition.
    SPEED_LOW_ALERT(16u), // Group 0. Start / stop when in alert condition.
    CAD_HIGH_ALERT(17u), // Group 0. Start / stop when in alert condition.
    CAD_LOW_ALERT(18u), // Group 0. Start / stop when in alert condition.
    POWER_HIGH_ALERT(19u), // Group 0. Start / stop when in alert condition.
    POWER_LOW_ALERT(20u), // Group 0. Start / stop when in alert condition.
    RECOVERY_HR(21u), // marker
    BATTERY_LOW(22u), // marker
    TIME_DURATION_ALERT(23u), // Group 1. Start if enabled mid activity (not required at start of activity). Stop when duration is reached. stop_disable if disabled.
    DISTANCE_DURATION_ALERT(24u), // Group 1. Start if enabled mid activity (not required at start of activity). Stop when duration is reached. stop_disable if disabled.
    CALORIE_DURATION_ALERT(25u), // Group 1. Start if enabled mid activity (not required at start of activity). Stop when duration is reached. stop_disable if disabled.
    ACTIVITY(26u), // Group 1.. Stop at end of activity.
    FITNESS_EQUIPMENT(27u), // marker
    LENGTH(28u), // Stop at end of each length.
    USER_MARKER(32u), // marker
    SPORT_POINT(33u), // marker
    CALIBRATION(36u), // start/stop/marker
    FRONT_GEAR_CHANGE(42u), // marker
    REAR_GEAR_CHANGE(43u), // marker
    RIDER_POSITION_CHANGE(44u), // marker
    ELEV_HIGH_ALERT(45u), // Group 0. Start / stop when in alert condition.
    ELEV_LOW_ALERT(46u), // Group 0. Start / stop when in alert condition.
    COMM_TIMEOUT(47u), // marker
    AUTO_ACTIVITY_DETECT(54u), // marker
    DIVE_ALERT(56u), // marker
    DIVE_GAS_SWITCHED(57u), // marker
    TANK_PRESSURE_RESERVE(71u), // marker
    TANK_PRESSURE_CRITICAL(72u), // marker
    TANK_LOST(73u), // marker
    RADAR_THREAT_ALERT(75u), // start/stop/marker
    TANK_BATTERY_LOW(76u), // marker
    TANK_POD_CONNECTED(81u), // marker - tank pod has connected
    TANK_POD_DISCONNECTED(82u), // marker - tank pod has lost connection

    /** Returned for any raw value not present in the profile. */
    INVALID(UByte.MAX_VALUE),
    ;

    public companion object {
        private val byValue: Map<UByte, Event> =
            entries.associateBy { it.value }

        /** Maps a raw FIT value onto its enum entry, falling back to [INVALID]. */
        public fun fromValue(value: UByte): Event =
            byValue[value] ?: INVALID
    }
}
